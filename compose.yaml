services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: settlement_funding
      POSTGRES_USER: settlement
      POSTGRES_PASSWORD: settlement
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: